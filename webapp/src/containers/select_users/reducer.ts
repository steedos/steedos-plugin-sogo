
function reducer(state: any = {}, action: any){
    // console.log("selectusers.reducer state", state)
    // console.log("selectusers.reducer action", action)
    // if (action.type === ORGANIZATIONS__STATE_CHANGE_ACTION) {
    //     // switch (action.partialStateName) {
    //     //     case 'loadDataSauce':
    //     //         return Object.assign({}, state, { rows: action.partialStateValue.records, totalCount: action.partialStateValue.totalCount });
    //     //     default:
    //     //         break;
    //     // }
    //     return {
    //         ...state,
    //         [action.partialStateName]: action.partialStateValue,
    //     };
    // }

    return state;
};

export default reducer;